## Conventions

- Always use named exports, the usage of `export default` is forbidden
- We prefer functional programming instead of OOP
- Whem writting functions, always use arrow functions
- Reduce the cognitive complexity when writing boolean conditions: always extract to constants to improve readibility in if statements
- Prefer pure functions, avoid mutation where practical.
- Favor composition over inheritance.
- Use camel case
